/*import React from "react";

function User(props) {
    const User = (props) => {
        return (
            <div>
                {props.name} {props.age}
            </div>
        );
    };    
    
}
export default User;
*/
export const User = (props) => {        
    return (
        <div>
            {props.name} {props.age}
        </div>
    )
};
